<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.snap.thirdear.MainActivity"
    tools:showIn="@layout/app_bar_main">


    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/waveImageView"
        android:src="@drawable/soundwave_quiet"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="52dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/stopped_msg"
        android:id="@+id/statusTextView"
        android:layout_alignTop="@+id/waveImageView"
        android:layout_alignEnd="@+id/quickviewBtn"
        android:textAlignment="center" />

    <ImageButton
        android:layout_width="300dp"
        android:layout_height="65dp"
        android:id="@+id/mainButton"
        android:src="@drawable/start"
        android:onClick="appControl"
        android:layout_below="@+id/waveImageView"
        android:layout_centerHorizontal="true" />

    <ImageButton
        android:layout_width="200dp"
        android:layout_height="65dp"
        android:id="@+id/quickviewBtn"
        android:onClick="quickView"
        android:layout_marginTop="66dp"
        android:src="@drawable/quick_view"
        android:layout_below="@+id/mainButton"
        android:layout_centerHorizontal="true" />


</RelativeLayout>
